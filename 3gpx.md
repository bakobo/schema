# sedi-age schema (bakobo/schema main 44c568a): the aggregate A list is unconstrained; every item accepts a string and there is no minItems/maxItems, positional schema or required threshold set, so an empty or arbitrary list of SAIDs validates as an age credential. Constrain it to the documented layout. From Copilot on bakobo/sedi-summit-nov26#1 (harness carries a byte copy).
kind: debt
created: 2026-09-24T15:44Z

