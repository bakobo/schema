# Add an aggregate-section check to schematools: recompute the AGID and per-block SAIDs with keri's Aggor and assert Aggor.verifyDisclosure on every 'A'-carrying example and disclosure. This is the check whose absence let sedi-age ship an aggregate that was Saider-consistent (so the linter passed) but failed the protocol's own verification -- found and fixed 2026-09-15 under this.i @lkfnoess. The linter currently computes and checks aggregate SAIDs with the SAME generic path, so it validates the corpus against itself. Pairs with tick 2nd5.
kind: todo
created: 2026-09-15T06:06Z

