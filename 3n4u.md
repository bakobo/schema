# Migrate face-to-face to ACDC v2 (@jruwvxnt). The largest non-SEDI schema (29 fields) and the richest genuine rules block, so @jnft7xse is satisfied -- the design work is elsewhere. It already requires a.u, so it becomes issuable the moment heti's nonce support meets a v2 envelope. THE ONE AGGREGATE CANDIDATE outside sedi-age per @jsmu322m: biometricHashes/biometricProtocol disclose on a different axis from the meeting facts (minutes, firstMet, modalities), so evaluate an A section -- and if adopted, element 1 must be a REQUIRED issuee block (prefixItems + minItems), the failure tick 6grq records for sedi-age. Close the a-level additionalProperties per @hoag7c7s.
kind: todo
created: 2026-09-15T03:02Z

