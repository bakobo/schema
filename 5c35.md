# Evaluate then ship (if it earns its place) a presentation-base schema, dossier-base style: the reusable rich-presentation shape -- I2I edges to one+ source credentials, an 'r' pointing at the SEDI governance framework, and a minimal interaction-attribute block -- that named verifier-pattern schemas (age-portrait admittance, KYC identity, voter-eligibility) specialize. The verifier's request stays an IPEX apply query, not a separate schema; the club's accepted-shape and the holder's presentation are one schema from two sides. Sketch first, build only if justified. this.i @sdp3wk / @sdc7xn.
kind: todo
created: 2026-07-16T20:17Z

- 2026-07-16T20:31Z OUTCOME of the sketch-first evaluation (2026-07-16): DEFER the base -- do not build presentation-base speculatively. Sketch settled its shape: a holder-issued, UNREGISTERED presentation ACDC with i=holder(Discloser), a={d,u,i:verifier(Disclosee),+pattern attrs}, e={named I2I edges {n:sourceSAID,s:sourceSchemaSAID,o:'I2I'}}, r=governance-framework SAID (required), and NO rd (one-time, not logged). Its real teeth would be: require a.i (disclosee binding), require r (a presentation with no gfw ref is rejected), and pin every edge o to const 'I2I'; it leaves which/how-many edges and the pattern attrs open. VERDICT: the base is too thin with only one pattern, so follow the repo's 'generic only once a second consumer justifies it' instinct -- (1) build the FIRST concrete verifier-pattern schema on demand (first candidate: sedi-present-age-portrait, pinning e.identity->sedi-id, e.age->sedi-age, a.over21, portrait disclosure alongside), then (2) EXTRACT presentation-base at the SECOND pattern, when the shared shape is proven not guessed. This tick stays open, effectively blocked on a second presentation pattern existing. Sketch + reasoning retained in this.i @sdp3wk; the reusable CLC terms are the separate governance-framework rules.json (tick 3e6n).
- 2026-07-16T21:10Z UPDATE 2026-07-16: the FIRST presentation pattern is now built -- sedi-present-age-portrait (holder-issued v1 ACDC, I2I edges to sedi-id + sedi-age, r -> SEDI governance framework; teeth: edge o const I2I, r required, rd disallowed). The presentation-base extraction (this tick) stays deferred until a SECOND pattern exists. this.i @sdgv7k / @sdp3wk.
