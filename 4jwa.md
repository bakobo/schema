# Migrate bindkey to ACDC v2 (@jruwvxnt). Second demonstrator: 5 fields, already camelCase. Envelope per @enr3eg; declare u in both slots per @jsmu322m -- it binds a PUBLIC key, so a watcher can confirm the credential by recomputing td; startDate/stopDate -> validFrom/validUntil per @pp4wv7pw; no r section exists, author one per @jnft7xse (the clause that matters: a bindkey does not assert the key is uncompromised); close the a-level additionalProperties @p3rk6d flagged, per @hoag7c7s. Rename example-bindkey.json -> example.json.
kind: todo
created: 2026-09-15T03:02Z
closed: 2026-09-15T05:19Z

