# When a RELEASED keri v2 can be pinned and differentially SAID-verified, revisit sedi-id: stamp a v2 version string and have the linter recompute the AGID (currently AGID authenticity is checked only by the keripy test, and aggregate examples are unversioned). this.i @sd2qfw / @m4vd7s.
kind: todo
created: 2026-07-16T19:22Z

- 2026-07-16T20:17Z Investigated 2026-07-16 (Explore agent): keri==2.0.0.dev5 is the latest PINNABLE v2 (the local dev6 build is not on PyPI). Under dev5: schematools imports unchanged, ZERO SAID drift across all 14 schemas + every example (incl. v1-versioned ones), and keri.core.Aggor (agid/disclose/verifyDisclosure) + Ilks.acg are present -- so the bump is proven safe AND aggregate-capable. BUT dev5 is a pre-release DEV build (no stable/rc 2.x exists on PyPI at all), and realizing the benefit still needs new linter code (integrate Aggor; stamp sedi-id acg with a v2 version string). DECISION (accepted by DHH): defer, stay on 1.2.13, keep this tick open until a non-dev 2.x release. this.i @sd2qfw / @m4vd7s.
- 2026-08-15T23:08Z The oracle pin has since moved to the v2-capable bakobo/keripy fork (this.i @h3or4x, 2026-08-15), differentially verified SAID-identical over the corpus - so the 'released keri v2' blocker is effectively gone; what remains is the sedi-id/sedi-age work itself (v2 version stamps, AGID recompute in the linter).
- 2026-09-15T06:06Z PARTIALLY CLOSED 2026-09-15 by this.i @lkfnoess. The version-stamp half is DONE: sedi-age is now a true ACDC v2 'acg', whose field domain has no optional fields, so 'v' is mandatory and every example carries a real v2 version string; @sd2qfw is lifted. The linter half REMAINS OPEN and is what this tick now tracks -- schematools still treats 'A' opaquely and does not recompute the AGID. That gap turned out to be load-bearing, not cosmetic: because the linter both computed and checked aggregate SAIDs with the generic Saider path, the published sedi-age aggregate was self-consistent against the linter while Aggor.verifyDisclosure returned False on it, and nothing could see it. The artifacts are now built with Aggor and verify, but the check that would have caught it still does not exist in schematools.
