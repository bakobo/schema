# IDEA: render the example galleries (gcd/examples/ and any others) with the cesrview React component (bakobo/cesrview — a client-side, pure-JS 'jwt.io for CESR' ACDC/credential viewer: print, copy/paste, hover, a11y) instead of link-to-raw-JSON, so the browsable site displays credentials richly. Would slot into schematools build-docs (generate a per-example page embedding the JSON + the component) + a nav entry. See this.i @g4tn7w (the gallery), @z5nc4d (the Zensical human layer, explicitly non-load-bearing), @o6bw3k (machine-first — the raw JSON stays the source of truth).
kind: todo
created: 2026-07-15T16:01Z

