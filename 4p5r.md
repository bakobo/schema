# Reconcile this registry with GLEIF's .well-known convention, now that bakobo.com publishes one too (2026-09-04). Three items, none decided. (1) TWO DIALECTS, NO RELATIONSHIP: this site's canonical manifest is /.well-known/acdc-schemas.json (@pv6k3d, @o6bw3k) while bakobo.com now serves GLEIF's shape -- host-meta.json plus a flat type-agnostic /.well-known/oobi/{said}/index.json plus a generated catalog. Same company, same ecosystem, two shapes. Adding a GLEIF-conformant surface HERE alongside the existing manifest is cheap, because the per-SAID OOBIs already exist: it is a host-meta.json and a flat mirror. Then Bakobo speaks both dialects on both origins and nothing has to be retired. https://schema.bakobo.com/.well-known/host-meta.json currently 404s. (2) federation.json's GLEIF entry is STALE: it records resolution 'oobi' with no manifest, but GLEIF-IT has published a machine catalog since 2026-08-05 at gleif-it.github.io/.well-known/oobi/index.json, and WebOfTrust publishes an identical surface from a byte-identical build script. Update the entry and add the manifest URL. (3) LAYER 3 IS UNBLOCKED: @f7dr3k deferred cross-registry SAID aggregation 'gated on peers exposing manifests'. That gate is open -- GLEIF and WebOfTrust both expose one now. Worth reopening the deferral rather than leaving it deferred by inertia. Background, verified from primary sources and written up: bakobo/infra docs/gleif-well-known.md, which also records that both PRs GLEIF's Message 44 cites are superseded and that no successor spec document exists.
kind: todo
created: 2026-09-05T19:52Z

- 2026-09-05T20:34Z 2026-09-05: item 2 DONE, merged as bakobo/schema#1. federation.json's GLEIF entry now reads resolution=static with a manifest and oobiPattern, and the notes record that weboftrust.github.io serves a byte-identical surface from the same build script. All three URLs verified 200 before commit. Items 1 (a GLEIF-conformant surface here alongside acdc-schemas.json) and 3 (@f7dr3k's Layer 3, whose 'gated on peers exposing manifests' condition this change documents as met) remain open. Left the 'WebOfTrust schema' entry alone deliberately -- it points at github.com/WebOfTrust/schema, a different repo from the WebOfTrust.github.io mirror, so 'no confirmed public host' may still be true of it.
