# Evaluate then ship (if it earns its place) a presentation-base schema, dossier-base style: the reusable rich-presentation shape -- I2I edges to one+ source credentials, an 'r' pointing at the SEDI governance framework, and a minimal interaction-attribute block -- that named verifier-pattern schemas (age-portrait admittance, KYC identity, voter-eligibility) specialize. The verifier's request stays an IPEX apply query, not a separate schema; the club's accepted-shape and the holder's presentation are one schema from two sides. Sketch first, build only if justified. this.i @sdp3wk / @sdc7xn.
kind: todo
created: 2026-07-16T20:17Z

