# Migrate sedi-guardian to ACDC v2 (@jruwvxnt), AFTER sedi-id and after the unmerged sedi-guardian-no-gcd branch is settled -- that branch withdraws the GCD composition and deletes the e.scope edge, and migrating around a recorded withdrawal manufactures conflicts (the reason @enr3eg left it untouched). It pins sedi-id's SAID and, on main, GCD's. 28 fields; effectiveDate/expiryDate -> validFrom/validUntil per @pp4wv7pw; already requires a.u. Also: 8 properties carry no type/oneOf/$ref, so the field-quality pass in @jruwvxnt has real work here.
kind: todo
created: 2026-09-15T03:02Z

