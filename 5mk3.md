# Migrate attestation to ACDC v2 (@jruwvxnt). First demonstrator: 5 fields, already camelCase, no cross-pins. Envelope ri->rd per @enr3eg; declare u top-level and in a (required there) per @jsmu322m -- it asserts a digest an observer may already hold, so the public-td guess applies; it declares NO r section at all, so author a rules.json per @jnft7xse; optional validUntil per @pp4wv7pw; additionalProperties already closed in 4 places, confirm against @hoag7c7s. Also rename example-attestation.json -> example.json so the linter's example/example_ref/example_said checks stop silently skipping it.
kind: todo
created: 2026-09-15T03:02Z

