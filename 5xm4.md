# GCD's e block requires 'd' (gcd.schema.json, e.oneOf[1].required = ['d','issuer']), but the fork's acdcmap emits NO block SAID for an edge section -- the same fact that made @enr3eg drop a.d from a.required. So an edge-bearing GCD built by the only builder there is would fail its own schema. Latent rather than observed: GCD's 'e' is optional and its example carries none, so nothing has exercised it, and heti refuses edge-bearing ACDCs by name today (heti tick 2esz). Surfaced while migrating ai-coder (@jruwvxnt), whose chained-to-coca gallery example failed on exactly this until e.d was made optional there. Fix is MINOR per @k3wm7d (relaxing a required list invalidates no valid instance): drop 'd' from the e arm's required, keep the property. Check face-to-face and the other edge-bearing schemas for the same shape when they migrate.
kind: debt
created: 2026-09-15T06:47Z

